# Pingwatch website

Commercial marketing site for Pingwatch.

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
npm run preview
```

The site is a Vite + React + TypeScript single page. Product copy matches the Pingwatch application (six monitor types, Docker and host deploy, Superstack demo).
